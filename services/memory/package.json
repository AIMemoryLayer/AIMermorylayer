{
  "name": "memory-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "build": "echo 'No build required for Python FastAPI'",
    "lint": "ruff check . && ruff format --check ."
  }
}
