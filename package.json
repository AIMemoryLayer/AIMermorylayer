{
  "name": "ai-memory-layer",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "services/*",
    "frontend/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.5.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
